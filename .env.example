# Neon Database Connection
# Get this from your Neon dashboard: https://console.neon.tech
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# Encryption key for sensitive fields (SSN, banking, etc.)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your-32-byte-encryption-key-here

# Cloudflare R2 Storage
# Get credentials from: https://dash.cloudflare.com → R2 → Manage R2 API Tokens
R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_BUCKET_NAME=optivo
# Public URL for your R2 bucket (set up custom domain or use R2.dev subdomain)
NUXT_PUBLIC_R2_URL=https://your-bucket.your-domain.com
